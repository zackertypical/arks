apiVersion: arks.scitix.ai/v1
kind: ArksToken
metadata:
  labels:
    app.kubernetes.io/name: arks-operator
    app.kubernetes.io/managed-by: kustomize
  name: arkstoken-sample
spec:
  # TODO(user): Add fields here
